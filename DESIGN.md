---
version: alpha
name: Markdown Leader
description: Local Markdown reader visual design system
colors:
  primary: "#0088ff"
  primary-dark: "#0091ff"
  primary-strong: "#1765e3"
  primary-strong-dark: "#5cb8ff"
  background: "#f7f8fa"
  background-dark: "#11151b"
  surface: "#ffffff"
  surface-dark: "#171c24"
  text: "#20242c"
  text-dark: "#e8edf5"
  muted: "#667085"
  muted-dark: "#98a2b3"
  border: "#dfe3e8"
  border-dark: "#2b3441"
  diagram-border: "#7b8798"
  diagram-border-dark: "#8996a8"
  hover: "#eff4ff"
  hover-dark: "#202a3a"
  focus-ring: "#6c8ccb"
  focus-ring-dark: "#9cb7e8"
  inline-code: "#edf0f4"
  inline-code-dark: "#11151b"
  code: "#0d1117"
  error: "#dc2626"
  error-dark: "#ff7b72"
typography:
  body:
    fontFamily: "system-ui, sans-serif"
    fontSize: 17px
    fontWeight: 400
    lineHeight: 1.72
  control:
    fontFamily: "system-ui, sans-serif"
    fontSize: 13px
    fontWeight: 400
    lineHeight: 1.4
  button:
    fontFamily: "system-ui, sans-serif"
    fontSize: 13px
    fontWeight: 600
    lineHeight: 1.4
  toc:
    fontFamily: "system-ui, sans-serif"
    fontSize: 14px
    fontWeight: 400
    lineHeight: 1.45
  metadata:
    fontFamily: "system-ui, sans-serif"
    fontSize: 11px
    fontWeight: 400
    lineHeight: 1.45
rounded:
  xs: 4px
  sm: 5px
  md: 6px
  control: 8px
  menu: 10px
  card: 14px
  full: 9999px
spacing:
  base: 4px
  xs: 4px
  sm: 8px
  md: 12px
  lg: 16px
  xl: 24px
components:
  button-primary:
    backgroundColor: "{colors.primary-strong}"
    textColor: "{colors.surface}"
    typography: "{typography.button}"
    rounded: "{rounded.control}"
    padding: 9px
  button-primary-dark:
    backgroundColor: "{colors.primary-strong-dark}"
    textColor: "{colors.background-dark}"
    typography: "{typography.button}"
    rounded: "{rounded.control}"
    padding: 9px
  search-field:
    backgroundColor: "{colors.background}"
    textColor: "{colors.text}"
    typography: "{typography.control}"
    rounded: "{rounded.control}"
    padding: 4px
    height: 34px
  select:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.text}"
    typography: "{typography.control}"
    rounded: "{rounded.control}"
    padding: 6px
    height: 32px
  document:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.text}"
    typography: "{typography.body}"
    rounded: "{rounded.card}"
    padding: 48px
    width: 920px
---
# Markdown Leader 디자인 기준

## Overview

Markdown Leader는 로컬 문서를 오래 읽는 데 맞춘 차분한 데스크톱 인터페이스다. 중립색 표면, 얇은 경계, 작은 둥근 모서리와 한 계열의 파란색으로 문서와 탐색 구조를 분명히 구분한다. 장식보다 긴 문서의 가독성, 조작 상태의 식별, Light·Dark 환경의 일관성을 우선한다.

이 파일은 시각 디자인 SSOT다. Google 규격과 문서별 책임은 [디자인과 프론트엔드 기준표](docs/standards/design_frontend_boundaries.md)를 따른다.

## Colors

중립 팔레트는 본문과 패널의 대비를 만들고 파란색은 링크, 선택, 입력 포커스와 주요 동작에만 사용한다. `System` 테마는 운영체제의 밝기 선호에 따라 Light 또는 Dark 값을 선택한다.

| 역할 | CSS 변수 | Light | Dark |
|---|---|---|---|
| 바탕 | `--ml-bg` | `#f7f8fa` | `#11151b` |
| 표면 | `--ml-surface` | `#ffffff` | `#171c24` |
| 기본 글자 | `--ml-text` | `#20242c` | `#e8edf5` |
| 보조 글자 | `--ml-muted` | `#667085` | `#98a2b3` |
| 경계 | `--ml-border` | `#dfe3e8` | `#2b3441` |
| 다이어그램 경계 | `--ml-diagram-border` | `#7b8798` | `#8996a8` |
| 선택·입력 테두리 | `--ml-accent` | `#0088ff` | `#0091ff` |
| 링크·강조 글자·주요 동작 | `--ml-accent-strong` | `#1765e3` | `#5cb8ff` |
| 포커스 링 | `--ml-focus-ring` | `#6c8ccb` | `#9cb7e8` |
| 가리킴·선택 바탕 | `--ml-hover` | `#eff4ff` | `#202a3a` |
| 문서 경고·주의 | `--ml-error` | `#dc2626` | `#ff7b72` |
| 인라인 코드 바탕 | `--ml-inline-code-bg` | `#edf0f4` | `#11151b` |

파란색 기준은 [Apple Human Interface Guidelines의 색상 참고값](https://developer.apple.com/tutorials/data/design/human-interface-guidelines/color.json)에서 가져왔다. Apple 참고값의 Increased Contrast는 Light `#1e6ef4`, Dark `#5cb8ff`다. 현재 웹 UI는 이 값에 동적으로 전환하지 않으며 표의 Light·Dark 값을 고정 디자인 토큰으로 사용한다. 흰색 위 `#1765e3`은 5.24:1, `#eff4ff` 위에서는 4.76:1 대비를 제공한다. 흰색 글자를 쓰는 주요 버튼에는 `#1765e3`을 사용한다.

검색 강조는 일반 일치 `#ffe66d`, 현재 일치 `#ff963b`, 파일 트리 일치 `#fff0b3`을 유지한다. 오류 글자는 `#dc2626`, 코드 블록은 `#0d1117` 표면과 `#e6edf3` 글자를 사용한다. 색만으로 선택이나 오류를 전달하지 않는다.

## Typography

- 읽기 화면, 사이드바와 컨트롤은 `--ml-font-family`를 상속한다. 기본값은 `system-ui, sans-serif`이며 사용자가 Pretendard를 선택하면 `Pretendard Variable, system-ui, sans-serif`를 사용한다.
- 본문 기본값은 17px/1.72이고 사용자 설정으로 크기를 바꿀 수 있다. 제목은 본문 대비 `2.2em`, `1.6em`, `1.3em` 단계와 1.3 줄높이를 사용한다.
- 설정과 파일 트리는 13px, 목차는 14px, 보조 안내는 11–12px를 사용한다.
- 동작 버튼의 텍스트는 `typography.button`의 굵기 600을 사용한다. 기본 글자 크기는 13px이며 구성 요소에 정해진 기존 크기·줄 높이는 유지한다. 입력값·일반 레이블의 `typography.control` 굵기 400과 구분한다.
- 코드에는 `Consolas, Menlo, Monaco`를 우선하고, 해당 글꼴에 없는 문자는 선택한 본문 글꼴로 대체한다.
- 선택 메뉴 항목은 같은 컨트롤 글꼴을 사용해 항목마다 폭이나 높이가 달라지지 않게 한다.

## Layout

- 데스크톱 사이드바는 300px, 본문 영역은 왼쪽 300px를 비운다. 850px 이하에서는 사이드바와 여백을 250px로 줄인다.
- 연속 본문 카드의 기본 최대 너비는 920px이며 사용자 설정에 따라 달라진다. 위·아래 48px, 좌·우 58px 여백을 사용한다. 좁은 화면에서는 좌·우 28px로 줄인다.
- 페이지형 문서는 210×297mm 표면과 사방 20mm 안쪽 여백을 사용한다. 페이지 사이 간격은 16px, 윤곽은 공통 border 색의 1px 선이다. 내용 배치 폭은 화면 크기와 무관하게 유지하며 좁은 화면에서도 안쪽 여백을 축소하지 않는다. 인쇄에서는 페이지 사이 간격과 종이 윤곽선을 제거한다.
- 4px를 기본 간격 단위로 사용한다. 기존 화면의 6px, 9px, 10px, 14px 값은 구성 요소의 밀도를 유지하는 경우에만 쓴다.
- 사이드바 전체 폭과 본문 시작 위치를 유지한다. 40px 세로 리본 옆에 탐색 패널을 두고, 패널 스크롤은 본문과 분리한다. 탐색 상단은 파일·목차 아이콘 탭과 검색 아이콘 버튼 순서다.
- 검색 필드는 한 줄 34px 높이다. 입력이 남은 너비를 차지하고 결과 수와 24px 이전·다음·닫기 버튼은 오른쪽에 고정된다. 300px와 250px 사이드바에서 겹치거나 넘치지 않아야 한다.
- 인쇄에서는 사이드바와 상태를 숨기고 본문의 테두리, 그림자와 최대 너비를 제거한다.

## Elevation & Depth

표면색과 1px 경계로 계층을 구분하고 그림자는 필요한 떠 있는 요소에만 사용한다. 활성 탭과 분할 버튼은 `0 1px 4px #0002`, 선택 메뉴는 `0 8px 24px #0002`, 본문 카드는 `0 12px 40px #1118270d` 한 겹을 사용한다. 유리 효과, 장식용 그라데이션과 두꺼운 그림자를 추가하지 않는다.

## Shapes

- 입력, 선택 버튼과 검색 필드는 8px, 선택 메뉴는 10px, 메뉴 항목과 포커스 링은 6px 모서리를 사용한다.
- 파일 행은 5px, 탭은 7px, 본문 카드는 14px, 이미지와 분할 컨트롤은 8px 모서리를 사용한다.
- 일반 입력과 선택 버튼은 높이 32px 이상, 1px 경계를 사용한다. 스위치는 34×20px 캡슐과 14px 원형 손잡이를 사용한다.
- 아이콘은 단색의 얇은 선과 둥근 끝을 사용한다. 검색 이동 아이콘은 16px SVG와 1.6px 선, 폴더·파일 아이콘은 16px와 1.4px 선을 사용한다.

## Components

### Notifications and connection state

일반 알림은 화면 우측 상단에서 위·오른쪽 16px 떨어진 작은 표면으로 표시한다. 최대 폭은 380px이며 좁은 화면에서는 좌우 16px 안쪽에 맞춘다. surface·text·border 토큰, 10px 모서리, 12px 안쪽 여백과 가벼운 그림자를 사용한다. 본문을 밀거나 backdrop을 만들지 않는다. 오류는 error 색의 작은 표식과 본문색 메시지를 함께 사용하고, 동작·닫기 버튼은 기존 버튼·포커스 기준을 따른다.

파일 연결에 문제가 있는 탭에는 제목 옆의 작은 오류 표식을 남긴다. 기존 핀·별·X의 고정 위치와 180px 탭 폭을 유지하며 색 외에 표식과 툴팁으로 상태를 구분한다. 복구 선택은 기존 공통 모달의 표면·경계·버튼을 사용하고 세 동작이 좁은 화면에서도 줄바꿈되어 표시될 수 있다.

### Action buttons

중립 동작 버튼은 기본 text 색과 굵기 600을 사용해 옅은 테두리 안에서도 동작을 쉽게 식별하게 한다. Light에서는 `#20242c`, Dark에서는 `#e8edf5`를 사용한다. 기존 버튼 크기, 높이, 안쪽 여백, 테두리와 모서리는 유지한다.

PDF 미리보기, 메모 패널, 확인 대화상자, 시작 화면의 열기 동작, 설정 분할 버튼, 정렬 기호, 코드 복사와 확장 팝업의 동작 텍스트에 적용한다. 선택된 분할 버튼은 text 글자색·surface 바탕·기존 그림자를 유지하고, 선택된 정렬 기호는 기존 accent 색을 유지한다. 두 경우 모두 굵기는 600으로 맞춘다. 주요 버튼은 기존 primary 바탕과 테마별 반전 글자색, 메모 추가 버튼은 기존 흑백 반전을 유지한다. 어두운 코드 표면의 복사 버튼은 코드 글자색 `#e6edf3`을 사용한다. 비활성 상태의 낮은 불투명도와 공통 hover·focus 표현도 유지한다.

문서명·파일·목차·라이브러리 탐색 행은 목록의 선택 위계를 따른다. 아이콘 전용 버튼에는 텍스트 굵기 기준을 적용하지 않는다.

이름 정렬 아이콘은 A를 왼쪽 위, Z를 오른쪽 아래에 배치한 단색 선형 SVG다. 사각 테두리와 화살표 없이 기존 18px 아이콘 크기·1.6px 선 두께를 사용한다. 오른쪽의 독립된 A/Z 상태 글자와 6px 간격을 유지한다.

PDF 인쇄 설정 안내는 문장 전체보다 선택값이 먼저 눈에 들어오도록 값만 굵게 표시한다. 나머지 안내는 기존 12px 보조 글자색과 일반 굵기를 유지한다. 강조 대상과 번역은 UI 콘텐츠 스타일 가이드를 따른다.

### Brand mark

로고는 `#0088ff` 둥근 사각형 안에 흰색 접힌 문서와 파란색 M을 조합한다. 문서의 접힌 면은 `#bfe5ff`로 구분한다. 글꼴·그라데이션·그림자 없이 굵은 도형으로 표현해 작은 도구 모음에서도 읽기 앱의 형태를 알아볼 수 있게 한다. 128×128 기준 바깥 여백은 8px, 파란 표면은 112×112px와 28px 모서리다. 리더와 팝업에서는 30px 정사각형으로 표시하고 옆의 제품명과 10px 간격을 둔다.

사이드바 상단 제품명 오른쪽의 버전은 11px 보조 글자색과 일반 굵기로 표시한다. 제품명과 기준선을 맞추고 6px 간격을 두며 배경 배지나 별도 행을 만들지 않는다.

### Reader and navigation

본문 카드는 바탕에서 분리된 표면으로 보이고, 표·인용·제목 경계는 공통 border 토큰을 쓴다. 폴더와 목차 행은 가리킴에 hover 표면을 사용한다. 현재 문서는 accent 글자, hover 표면과 굵기 600으로 표시한다. 현재 목차는 accent 글자와 굵기 700을 사용한다.

파일 목록의 보조 안내는 루트 이름 아래에 11px 일반 굵기와 muted 색상을 사용하고, 좌우 8px·아래 8px 여백으로 루트 이름 시작선에 맞춘다.

### Search

검색 필드는 입력 자체의 경계를 없애고 컨테이너의 1px 경계 하나만 사용한다. 입력 포커스는 이 경계를 accent로 바꾸며 바깥 링을 추가하지 않는다. 조작 버튼은 기본 배경을 투명하게 두고 가리킴에 hover 표면을 사용한다. 비활성 버튼은 40% 불투명도로 보인다. 검색 강조에는 테두리나 외곽선을 추가하지 않는다.

### Extended Markdown

문서의 강조 표시는 `#fff0b3` 바탕과 `#624600` 글자를 사용하며 테두리를 두지 않는다. 삽입 표시는 본문 글꼴·색상과 밑줄, 위·아래 첨자는 작은 글자와 기준선 이동으로 구분한다. 이모지는 운영체제의 Unicode 글리프를 사용한다.

문서 안 목차는 본문 흐름 안의 링크 목록으로 표시한다. 본문 대비 0.94em 글자, 왼쪽 2px 공통 경계와 16px 여백, 단계별 16px 들여쓰기를 사용한다. 카드나 그림자를 추가하지 않고 링크·키보드 포커스는 공통 스타일을 따른다.

주의·팁 박스는 4px 왼쪽 경계, 12px·16px 안쪽 여백, 공통 바탕색을 사용한다. 제목은 0.94em·굵기 600이며 일반 안내는 accent-strong, 경고·주의는 error 토큰과 제목으로 구분한다. Dark 문서 경고에는 대비를 유지하는 error-dark를 사용한다. 사용자 제목은 본문 글꼴을 따른다.

각주 목록은 위쪽 1px 공통 경계, 16px 여백, 0.9em 글자이며 참조·복귀 링크는 공통 링크·포커스 스타일을 쓴다. 용어 정의는 용어 굵기 600과 설명의 24px 들여쓰기로 구분한다.

수식 원문 대기 영역은 기존 인라인 코드 글꼴·색상을 사용하고 줄바꿈을 보존한다. 완성된 수식은 KaTeX 전용 글꼴과 현재 본문색을 사용한다. 블록 수식과 다이어그램은 문서 폭 안에서 가로 스크롤하며 페이지 전체를 넓히지 않는다. Mermaid 도형은 공통 surface·text·accent-strong과 선택된 본문 글꼴을 따르며, 테두리는 전용 diagram-border 토큰을 쓴다. 밝은 테마는 진한 회색, 어두운 테마는 밝은 회색으로 구분하고 시퀀스 다이어그램의 세로 생명선에도 적용한다. 일반 UI 경계색과 분리해 도형이 배경에서 또렷하게 구분되도록 한다. 다이어그램 원문 접기 영역과 실패 안내는 12px 보조 글자색을 사용하고 원문 코드 블록은 기존 스타일을 유지한다.

### Selects and settings

설정 컨트롤과 단축키 안내는 `--ml-border` 색의 1px 실선으로 구분한다. 선은 설정 영역의 좌우 14px 안쪽 여백을 따르고, 선 위아래 간격은 10px로 둔다.

선택 버튼은 현재 값과 오른쪽 화살표를 표시하고 기본 144px 폭을 사용한다. 리본 옆 설정 패널에서는 최대 120px로 제한한다. 좁은 화면의 숫자 입력은 72px로 줄여 레이블과 값을 한 줄에 유지한다. 펼친 메뉴는 버튼에서 4px 떨어지며 버튼보다 좁지 않다. 항목은 높이 32px 이상, 좌·우 10px 여백을 사용한다. 선택 항목은 hover 표면, accent 글자, 굵기 600과 체크 표시를 함께 사용한다. 설정의 테마·읽기 폭에는 기존 분할 버튼, 켜기·끄기에는 스위치, 범위 조절에는 현재 값이 보이는 슬라이더를 사용한다.

확장 팝업은 300px 너비, 18px 안쪽 여백과 14px/1.55 글자를 사용한다. 주요 버튼은 전체 너비, 위 10px 간격, 8px 모서리, 9px×12px 안쪽 여백, `#1765e3` 바탕과 흰색 글자를 사용한다.

### Focus and states

텍스트·숫자 입력과 닫힌 선택 필드는 추가 링 없이 accent 경계로 포커스를 표시한다. 버튼, 링크, 접기 제목과 슬라이더는 2px focus-ring 외곽선과 2px 간격을 사용한다. 스위치는 같은 링과 간격을 적용하면서 캡슐 모양을 유지한다. 메뉴 항목은 같은 2px 링을 안쪽으로 표시한다. 비활성 상태는 낮은 불투명도로 표시한다. 선택은 색 외에 체크, 눌림 또는 글자 굵기로 구분한다.

### Motion

선택 메뉴와 검색은 지연, 페이드나 슬라이드 없이 즉시 표시한다. 스위치와 폴더 화살표는 120ms 상태 전환을 사용한다. 새 움직임은 `prefers-reduced-motion`에서 제거한다.

### Ribbon and reader entry

세로 리본은 폴더 열기·파일 열기·설정 아이콘을 위에서부터 붙여 배치한다. 설정은 바닥으로 밀지 않고 명령 목록의 마지막에 둔다. 리본 폭은 40px, 아이콘 버튼은 32px 정사각형이며 18px 선 아이콘과 공통 hover·focus 토큰을 사용한다. 파일·목차 탭의 선택은 배경과 아이콘 색으로 표시한다. 세로 리본 툴팁은 아이콘 오른쪽 6px 간격에 세로 중앙 정렬하고, 가로 탐색 줄의 툴팁은 아이콘 아래 6px 간격에 가로 중앙 정렬한다. hover와 키보드 focus에서 표시하며, 12px 글자·6px 모서리·표면과 반대 명도의 배경을 사용한다.

본문 찾기는 기존 문서 카드 위 56px 여백의 세로 중앙에 34px 높이의 떠 있는 입력창으로 표시한다. 본문 크기와 위치를 밀지 않으며 검색창 왼쪽 경계는 본문 카드 왼쪽 테두리에 맞추고 화면 크기가 바뀌면 다시 정렬한다. 폴더 검색 입력과 결과는 파일 패널 안에 둔다. 검색 중에도 일반 알림은 Notifications and connection state의 우측 상단 배치를 따른다.

문서가 없는 시작 화면은 기존 본문 영역 안 중앙에 안내와 폴더 열기·파일 열기 버튼을 왼쪽부터 순서대로 표시한다. 안내 문구의 내용과 표시 위치는 [UI 콘텐츠 스타일 가이드](docs/standards/ui_content_style_guide.md)를 따른다. 드래그 중에는 1px 점선 경계를 accent로 강조하고 hover 배경을 쓴다. 기존 문서를 연 상태의 본문 크기·여백·서체는 유지한다.


### Document tabs

문서 탭 줄은 현재 본문 카드 바로 위의 본문 위 32px 높이와 연결 경계 1px 겹침을 가진 한 줄로 배치한다. 본문 카드의 시작 위치(위 56px), 폭, 안쪽 여백은 그대로 유지한다. 탭 줄은 카드의 왼쪽 테두리와 폭을 따르고, 활성 탭은 surface 배경과 열린 아래 경계로 본문과 연결한다. 카드 상단 모서리는 탭이 있을 때 직각으로 연결한다. 탭 줄은 스크롤 중에도 위쪽에 유지한다. 목차·제목 링크의 스크롤 여백은 68px로 두어 제목이 탭 뒤에 가려지지 않게 한다.

개별 탭은 고정 180px 폭, 12px 글자와 한 줄 말줄임을 사용한다. X는 활성·hover·키보드 focus 탭에서 보이며 키보드 포커스에는 기존 focus-ring을 사용한다. 탭 동작은 [지원 기능](README.ko.md#주요-기능), 툴팁 정보는 [UI 콘텐츠 스타일 가이드](docs/standards/ui_content_style_guide.md), DOM 상태와 키보드 처리는 [프론트엔드 원칙](docs/standards/design_frontend_boundaries.md#프론트엔드-원칙)을 따른다.

넘치는 탭은 가로 스크롤하며 두 줄로 늘리지 않는다. 좌우 이동 버튼은 넘칠 때만 표시하고 파일 열기용 + 버튼은 탭이 적으면 마지막 탭 바로 옆에 두고, 탭이 넘칠 때는 줄 오른쪽 끝에 유지한다. 본문 찾기는 같은 탭 줄 위에 플로팅으로 표시하며 탭을 가려도 된다. 검색창과 탭의 왼쪽 경계는 본문 카드와 일치한다.


### Document loading

로딩 표시는 150ms 지연 후 제목 한 줄과 본문 몇 줄의 정적인 스켈레톤으로 나타낸다. 제목 막대 높이는 H1의 글자 크기 2.2em × 줄 높이 1.3, 본문 막대 높이는 본문 줄 높이 1.72em을 따르며 선택한 글자 크기에 비례한다. 색상은 공통 border 토큰, 모서리는 4px이며 반짝임·페이드·로딩 바를 추가하지 않는다. 스켈레톤은 카드 높이를 늘리지 않고, 스크롤한 위치에서도 보이며 카드 밖으로 넘치지 않는다. 카드 높이 유지와 예약 표시 정리는 [프론트엔드 원칙](docs/standards/design_frontend_boundaries.md#프론트엔드-원칙), 문서 전환은 [지원 기능](README.ko.md#주요-기능)을 따른다.

탭 줄 위의 24px 영역은 불투명한 공통 바탕색으로 덮어, 스크롤한 본문이 탭 위로 비쳐 보이지 않게 한다. 본문과 탭의 기존 위치는 유지한다.

### Review controls and panels

즐겨찾기된 탭의 별은 비활성 탭에서도 고정 핀과 같이 text 색·100% 불투명도로 표시한다. 즐겨찾기되지 않은 별은 기존 활성·hover·focus 표시를 따르며, 탭 너비와 별의 고정 위치는 유지한다.

리본은 40px 폭으로 화면 왼쪽에 고정한다. 로고는 리본 상단 58px 영역 중앙의 26px 정사각형이며 접기·펼치기와 무관하게 위치를 유지한다. 제품명·버전은 리본 오른쪽의 기존 패널 상단에 배치한다. 탐색 패널을 접으면 리본 40px만 남기며 본문 카드와 탭은 같은 왼쪽 경계를 따른다. 리본 아이콘은 기존 32px 버튼·18px 선 아이콘 규칙을 유지한다. 툴팁은 기존 12px 글자와 표시 방향을 유지하고 안쪽 여백을 상하 8px·좌우 12px로 둔다. 파일 탐색기·즐겨찾기·최근 항목에서 잘린 이름이나 경로의 툴팁은 해당 행의 오른쪽 경계에 맞춰 정렬한다. 세 목록은 같은 툴팁을 사용하며 Light에서는 text 배경과 surface 글자, Dark에서는 밝은 text 배경과 어두운 surface 글자를 사용한다. 표시 정보는 목록별로 유지한다.

펼친 데스크톱 사이드바는 300px을 기본·최소 폭으로 사용하고, 오른쪽 경계를 드래그해 화면 폭의 45%와 520px 중 작은 값까지 넓힐 수 있다. 경계 전체의 10px 영역은 평소 보이지 않으며 마우스가 들어온 높이에 기존 아이콘 버튼과 같은 32px 원형 `‹ ›` 손잡이를 표시한다. 두 기호는 버튼 텍스트처럼 기본 text 색과 굵기 700으로 또렷하게 표시한다. 손잡이는 포인터 높이를 따르되 화면 위·아래 18px 안에 제한하고, 클릭만으로는 동작하지 않으며 키보드 조절을 제공하지 않는다. 드래그 중 사이드바·본문 시작선·드롭 안내를 함께 실시간 이동하고 마우스를 놓을 때 폭을 로컬 저장한다. 패널을 접으면 손잡이를 숨기고 40px 리본만 남기며, 다시 펼치면 저장 폭을 복원한다. 850px 이하에서는 손잡이를 숨기고 기존 250px 폭을 사용한다.

파일·목차·최근·즐겨찾기 패널은 세로로만 스크롤한다. 긴 파일명과 경로는 가로 스크롤바를 만들지 않고 가용 폭까지 한 줄로 표시한 뒤 말줄임하며 기존 툴팁으로 전문을 확인한다.

최근 항목·즐겨찾기는 기존 왼쪽 패널 안에 표시하며 항목에는 13px 파일명과 11px 보조 경로를 사용한다. 목록 항목 높이는 최소 44px이며 hover·focus는 기존 토큰을 따른다.

문서 탭은 180px 고정 폭을 유지한다. 핀·별·X는 20px 버튼과 14px 아이콘으로 오른쪽 고정된 자리를 확보한다. 현재 탭의 핀·별은 비활성일 때 muted 색과 낮은 불투명도, 활성일 때 text 색과 100% 불투명도를 사용한다. 컬러 강조는 추가하지 않는다. 고정된 비활성 탭도 핀 상태를 식별할 수 있다. 비활성 탭의 오른쪽에는 위아래 9px 여백의 1px border 구분선을 둔다. 활성 탭 좌우 하단은 7px 바깥 곡선으로 본문 표면에 연결하며 인접 구분선이 곡선을 가르지 않게 한다. 드래그 위치는 2px focus-ring 세로선으로 표시한다. 고정 탭은 일반 탭 앞에 모아 표시한다.

고정 탭 닫기 확인은 제품 스타일의 dialog로 표시한다. 최대 폭 360px, 표면색과 본문색, 12px 모서리, 20px 안쪽 여백과 기존 버튼·focus 토큰을 사용한다. 확인 모달에만 옅은 어두운 backdrop을 사용한다. 메모 패널에는 backdrop이 없다.

메모 패널은 화면 오른쪽에 고정된 340px 폭의 표면으로 본문 위에 겹친다. 작은 화면에서는 리본을 제외한 가용 폭 안에 제한한다. 본문 배치·읽기 폭·스크롤을 밀지 않는다. 왼쪽 1px border와 약한 그림자로 구분한다. 헤더 아래 목록은 독립 스크롤한다. 메모 카드는 12px 안쪽 여백, 번호 배지, 한 줄 말줄임 대상 인용문과 13px 메모 본문을 쓴다. 카드 하단 동작은 오른쪽 정렬하며 수정 중에도 같은 카드 자리를 유지한다. 현재 메모는 focus-ring 경계와 hover 배경으로 표시한다. 번호·정렬·연결을 잃은 메모의 처리는 [지원 기능](README.ko.md#주요-기능), 제품 문구는 [UI 콘텐츠 스타일 가이드](docs/standards/ui_content_style_guide.md)를 따른다.

작은 메모 추가 버튼의 가로 중앙은 포인터 종료 위치에 맞추고 아래 6px 간격을 둔다. 버튼은 화면 가장자리 안에 제한한다. 추가 버튼은 Light에서 검정 배경과 흰 글자, Dark에서 흰 배경과 검정 글자를 쓴다. 메모 숫자는 선택 범위 마지막 글자의 오른쪽 위에 22px 원형 버튼으로 표시한다. 우측 패널이 덮더라도 위치를 옮기지 않는다. 기본 배지는 muted 배경과 surface 숫자, hover 배지는 text 배경과 surface 숫자를 사용해 테마별 대비를 유지한다. 선택 배지는 primary-strong 배경과 surface 숫자를 쓴다. 선택된 메모의 원문 범위는 accent의 20% 혼합 배경과 focus-ring 경계로 강조한다. 메모 편집기는 UI 공통 textarea·버튼 토큰을 사용하며 사용자 작성 텍스트는 text로 표시한다. 표시·선택 동작은 F-033, 레이아웃 변경에 따른 좌표 갱신은 프론트엔드 원칙을 따른다.

## Do's and Don'ts

- 새 값을 추가하기 전에 중립 표면과 공통 `--ml-*` 토큰을 사용한다.
- accent 색상은 상호작용, 선택, 링크와 화면에서 가장 중요한 동작에만 사용한다.
- Light, Dark, 가리킴, 선택, 비활성, 키보드 포커스 외형을 확인한다.
- 일반 글자는 4.5:1 이상, 시각적 포커스 표시는 3:1 이상의 대비를 유지한다.
- 선택, 오류 또는 포커스를 색만으로 표시하지 않는다.
- 읽기 UI에 장식용 그라데이션, 유리 효과, 두꺼운 그림자나 큰 움직임을 추가하지 않는다.
- 같은 역할에 다른 모서리, 메뉴, 탭, 스위치 또는 포커스 패턴을 만들지 않는다.
