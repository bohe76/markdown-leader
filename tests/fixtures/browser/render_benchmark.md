# 렌더링 성능 비교 문서

동일한 문서로 모든 단계를 측정합니다.

## 문서 섹션 1

외부 파일을 읽는 Markdown 문서입니다. **굵은 글자**, *기울임*, ==강조==, :sparkles:, H~2~O와 19^th^를 확인합니다. 프로젝트 상태와 검증 결과를 읽고 다음 작업을 준비합니다.

| 항목 | 상태 | 설명 |
|---|---|---|
| 읽기 | 정상 | 로컬 파일 |
| 갱신 | 정상 | 변경 반영 |

```js
const result = { status: "ok", count: 3 };
```

## 문서 섹션 2

외부 파일을 읽는 Markdown 문서입니다. **굵은 글자**, *기울임*, ==강조==, :sparkles:, H~2~O와 19^th^를 확인합니다. 프로젝트 상태와 검증 결과를 읽고 다음 작업을 준비합니다.

| 항목 | 상태 | 설명 |
|---|---|---|
| 읽기 | 정상 | 로컬 파일 |
| 갱신 | 정상 | 변경 반영 |

```js
const result = { status: "ok", count: 3 };
```

## 문서 섹션 3

외부 파일을 읽는 Markdown 문서입니다. **굵은 글자**, *기울임*, ==강조==, :sparkles:, H~2~O와 19^th^를 확인합니다. 프로젝트 상태와 검증 결과를 읽고 다음 작업을 준비합니다.

| 항목 | 상태 | 설명 |
|---|---|---|
| 읽기 | 정상 | 로컬 파일 |
| 갱신 | 정상 | 변경 반영 |

```js
const result = { status: "ok", count: 3 };
```

## 문서 섹션 4

외부 파일을 읽는 Markdown 문서입니다. **굵은 글자**, *기울임*, ==강조==, :sparkles:, H~2~O와 19^th^를 확인합니다. 프로젝트 상태와 검증 결과를 읽고 다음 작업을 준비합니다.

| 항목 | 상태 | 설명 |
|---|---|---|
| 읽기 | 정상 | 로컬 파일 |
| 갱신 | 정상 | 변경 반영 |

```js
const result = { status: "ok", count: 3 };
```

## 문서 섹션 5

외부 파일을 읽는 Markdown 문서입니다. **굵은 글자**, *기울임*, ==강조==, :sparkles:, H~2~O와 19^th^를 확인합니다. 프로젝트 상태와 검증 결과를 읽고 다음 작업을 준비합니다.

| 항목 | 상태 | 설명 |
|---|---|---|
| 읽기 | 정상 | 로컬 파일 |
| 갱신 | 정상 | 변경 반영 |

```js
const result = { status: "ok", count: 3 };
```

## 문서 섹션 6

외부 파일을 읽는 Markdown 문서입니다. **굵은 글자**, *기울임*, ==강조==, :sparkles:, H~2~O와 19^th^를 확인합니다. 프로젝트 상태와 검증 결과를 읽고 다음 작업을 준비합니다.

| 항목 | 상태 | 설명 |
|---|---|---|
| 읽기 | 정상 | 로컬 파일 |
| 갱신 | 정상 | 변경 반영 |

```js
const result = { status: "ok", count: 3 };
```

## 문서 섹션 7

외부 파일을 읽는 Markdown 문서입니다. **굵은 글자**, *기울임*, ==강조==, :sparkles:, H~2~O와 19^th^를 확인합니다. 프로젝트 상태와 검증 결과를 읽고 다음 작업을 준비합니다.

| 항목 | 상태 | 설명 |
|---|---|---|
| 읽기 | 정상 | 로컬 파일 |
| 갱신 | 정상 | 변경 반영 |

```js
const result = { status: "ok", count: 3 };
```

## 문서 섹션 8

외부 파일을 읽는 Markdown 문서입니다. **굵은 글자**, *기울임*, ==강조==, :sparkles:, H~2~O와 19^th^를 확인합니다. 프로젝트 상태와 검증 결과를 읽고 다음 작업을 준비합니다.

| 항목 | 상태 | 설명 |
|---|---|---|
| 읽기 | 정상 | 로컬 파일 |
| 갱신 | 정상 | 변경 반영 |

```js
const result = { status: "ok", count: 3 };
```

## 문서 섹션 9

외부 파일을 읽는 Markdown 문서입니다. **굵은 글자**, *기울임*, ==강조==, :sparkles:, H~2~O와 19^th^를 확인합니다. 프로젝트 상태와 검증 결과를 읽고 다음 작업을 준비합니다.

| 항목 | 상태 | 설명 |
|---|---|---|
| 읽기 | 정상 | 로컬 파일 |
| 갱신 | 정상 | 변경 반영 |

```js
const result = { status: "ok", count: 3 };
```

## 문서 섹션 10

외부 파일을 읽는 Markdown 문서입니다. **굵은 글자**, *기울임*, ==강조==, :sparkles:, H~2~O와 19^th^를 확인합니다. 프로젝트 상태와 검증 결과를 읽고 다음 작업을 준비합니다.

| 항목 | 상태 | 설명 |
|---|---|---|
| 읽기 | 정상 | 로컬 파일 |
| 갱신 | 정상 | 변경 반영 |

```js
const result = { status: "ok", count: 3 };
```

## 문서 섹션 11

외부 파일을 읽는 Markdown 문서입니다. **굵은 글자**, *기울임*, ==강조==, :sparkles:, H~2~O와 19^th^를 확인합니다. 프로젝트 상태와 검증 결과를 읽고 다음 작업을 준비합니다.

| 항목 | 상태 | 설명 |
|---|---|---|
| 읽기 | 정상 | 로컬 파일 |
| 갱신 | 정상 | 변경 반영 |

```js
const result = { status: "ok", count: 3 };
```

## 문서 섹션 12

외부 파일을 읽는 Markdown 문서입니다. **굵은 글자**, *기울임*, ==강조==, :sparkles:, H~2~O와 19^th^를 확인합니다. 프로젝트 상태와 검증 결과를 읽고 다음 작업을 준비합니다.

| 항목 | 상태 | 설명 |
|---|---|---|
| 읽기 | 정상 | 로컬 파일 |
| 갱신 | 정상 | 변경 반영 |

```js
const result = { status: "ok", count: 3 };
```

## 문서 섹션 13

외부 파일을 읽는 Markdown 문서입니다. **굵은 글자**, *기울임*, ==강조==, :sparkles:, H~2~O와 19^th^를 확인합니다. 프로젝트 상태와 검증 결과를 읽고 다음 작업을 준비합니다.

| 항목 | 상태 | 설명 |
|---|---|---|
| 읽기 | 정상 | 로컬 파일 |
| 갱신 | 정상 | 변경 반영 |

```js
const result = { status: "ok", count: 3 };
```

## 문서 섹션 14

외부 파일을 읽는 Markdown 문서입니다. **굵은 글자**, *기울임*, ==강조==, :sparkles:, H~2~O와 19^th^를 확인합니다. 프로젝트 상태와 검증 결과를 읽고 다음 작업을 준비합니다.

| 항목 | 상태 | 설명 |
|---|---|---|
| 읽기 | 정상 | 로컬 파일 |
| 갱신 | 정상 | 변경 반영 |

```js
const result = { status: "ok", count: 3 };
```

## 문서 섹션 15

외부 파일을 읽는 Markdown 문서입니다. **굵은 글자**, *기울임*, ==강조==, :sparkles:, H~2~O와 19^th^를 확인합니다. 프로젝트 상태와 검증 결과를 읽고 다음 작업을 준비합니다.

| 항목 | 상태 | 설명 |
|---|---|---|
| 읽기 | 정상 | 로컬 파일 |
| 갱신 | 정상 | 변경 반영 |

```js
const result = { status: "ok", count: 3 };
```

## 문서 섹션 16

외부 파일을 읽는 Markdown 문서입니다. **굵은 글자**, *기울임*, ==강조==, :sparkles:, H~2~O와 19^th^를 확인합니다. 프로젝트 상태와 검증 결과를 읽고 다음 작업을 준비합니다.

| 항목 | 상태 | 설명 |
|---|---|---|
| 읽기 | 정상 | 로컬 파일 |
| 갱신 | 정상 | 변경 반영 |

```js
const result = { status: "ok", count: 3 };
```

## 문서 섹션 17

외부 파일을 읽는 Markdown 문서입니다. **굵은 글자**, *기울임*, ==강조==, :sparkles:, H~2~O와 19^th^를 확인합니다. 프로젝트 상태와 검증 결과를 읽고 다음 작업을 준비합니다.

| 항목 | 상태 | 설명 |
|---|---|---|
| 읽기 | 정상 | 로컬 파일 |
| 갱신 | 정상 | 변경 반영 |

```js
const result = { status: "ok", count: 3 };
```

## 문서 섹션 18

외부 파일을 읽는 Markdown 문서입니다. **굵은 글자**, *기울임*, ==강조==, :sparkles:, H~2~O와 19^th^를 확인합니다. 프로젝트 상태와 검증 결과를 읽고 다음 작업을 준비합니다.

| 항목 | 상태 | 설명 |
|---|---|---|
| 읽기 | 정상 | 로컬 파일 |
| 갱신 | 정상 | 변경 반영 |

```js
const result = { status: "ok", count: 3 };
```

## 문서 섹션 19

외부 파일을 읽는 Markdown 문서입니다. **굵은 글자**, *기울임*, ==강조==, :sparkles:, H~2~O와 19^th^를 확인합니다. 프로젝트 상태와 검증 결과를 읽고 다음 작업을 준비합니다.

| 항목 | 상태 | 설명 |
|---|---|---|
| 읽기 | 정상 | 로컬 파일 |
| 갱신 | 정상 | 변경 반영 |

```js
const result = { status: "ok", count: 3 };
```

## 문서 섹션 20

외부 파일을 읽는 Markdown 문서입니다. **굵은 글자**, *기울임*, ==강조==, :sparkles:, H~2~O와 19^th^를 확인합니다. 프로젝트 상태와 검증 결과를 읽고 다음 작업을 준비합니다.

| 항목 | 상태 | 설명 |
|---|---|---|
| 읽기 | 정상 | 로컬 파일 |
| 갱신 | 정상 | 변경 반영 |

```js
const result = { status: "ok", count: 3 };
```

## 추가 문법

> [!NOTE]
> 읽기 전용 문서입니다.

> [!WARNING]
> 원본 파일은 변경하지 않습니다.

각주가 있는 문장입니다.[^note]

[^note]: 확인에 사용한 상세 설명입니다.

렌더링
: 문서를 읽을 수 있는 형태로 표시하는 작업.

인라인 수식 $E=mc^2$를 표시합니다.

$$
\frac{1}{2} + \sum_{i=1}^{n} i
$$

```mermaid
flowchart LR
 A[문서 열기] --> B[Markdown 해석]
 B --> C[읽기 화면]
```
